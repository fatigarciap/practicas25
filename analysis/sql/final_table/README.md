# Final Table SQL

Dominio para ensamblaje de la tabla longitudinal final lista para modelado.

Script canonico actual:

- `analysis/pipeline_final/06_final_table/06_01_longitudinal_cohort_model_ready.sql`

PENDIENTE antes de ejecucion reproducible:

- Resolver referencias no clean: `clinical_domains_sci` e `improvement_flags`.
